# Monte Carlo Simulation for Stock/Option Prices

A simple Python GUI based app that takes a ticker and uses the **yfinance** library to run a Monte Carlo simulation and generate the appropriate plot.
